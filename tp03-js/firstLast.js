//Déclarez un tableau d’entiers
let array = [] ;

//On calcule une valeur booléenne qui contrôle le tableau de la sorte :

//    let bool = false;
//    if (array[0] && (array[0])===array[array.length -1]) {
//        bool=true;    
//    }
let bool =(array[0] && (array[0])===array[array.length -1])?true:false;
console.log(bool);
